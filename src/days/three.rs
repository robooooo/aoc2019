use std::collections::HashMap;
use crate::utils;

fn one() -> utils::Result<i32> {
    let input: Vec<String> = utils::get_lines(utils::path("three.txt"))?;
    let map: HashMap<(i32, i32), bool> = HashMap::new();
    

    Ok(0)
}